---
tags: [sals3, turnover, handoff, market-rules, pricing, opus-5]
aliases: [Market Rules Turnover Prompt 2026-08-14]
created: 2026-08-14
updated: 2026-08-14
status: session-note
authority: handoff-record
owner_approved: false
related:
  - "[[sals3-turnover-prompt-template]]"
  - "[[hot]]"
  - "[[ADR-015-commercial-pricing-governance-category-product-and-fx-adjustments]]"
  - "[[sals3-portal-seller-market-configuration]]"
  - "[[sals3-session-2026-08-11-part34-category-margin-and-fx-policy]]"
  - "[[sals3-session-2026-08-13-part41-market-rules-profile-fix-and-pricing-rework]]"
---

# Turnover prompt — 2026-08-14, Market Rules focus, handed to Opus 5

Generated by the [[sals3-turnover-prompt-template]] procedure. Verified against real `git`/code state on 2026-08-14 before writing — see the four vault updates this same procedure produced: [[hot]], [[ADR-015-commercial-pricing-governance-category-product-and-fx-adjustments]] (amended), [[sals3-portal-seller-market-configuration]] (updated), and the new [[sals3-session-2026-08-13-part41-market-rules-profile-fix-and-pricing-rework]] session note.

**Not yet committed/pushed** — see "GIT AND FILE-SAFETY RULES" below.

````text
You are taking over the Sals3 project from a previous agent, specifically to work on
Market Rules (`sals3-portal` `/market-rules`) — per-seller market profile setup and
category/product pricing governance.

CURRENT DATE / CONTEXT
- Date: 2026-08-14.
- Communicate naturally in Taglish unless technical precision is clearer in English.
- Do not behave like a yes-man. Challenge weak logic with evidence and explain tradeoffs.
- The team is AJ (Lead Architect/Programmer, full-stack), Bogs (Senior Developer, full-stack,
  best friends and co-developers), and Robin (Marketing Manager). None of them is "Sals3
  Leadership/Owner/Board" - they are staff; there is a separate boss/owner above them.
- AJ works remotely on a Mac (nickname "Supot"); Bogs works on Windows. Say "Supot" in your
  opening line whenever you detect Bogs is talking about AJ's Mac-side work.

PRIMARY WORKSPACES

Obsidian vault (second brain):
E:\sals3-ecommerce  (vault root is the `docs/` subfolder — do not open the repo root as the vault)

Vault remote:
origin = github.com/Sals3-Official/sals3-ecommerce

Vault branch:
docs/admin-portal-part39 (matches origin/docs/admin-portal-part39 at commit 0043d7e)

Vault working-tree state — READ THIS BEFORE TOUCHING THE VAULT:
The vault has a LARGE dirty working tree right now: ~14 modified tracked files and ~13
untracked new files, spanning several unrelated pieces of work (Admin Portal, CJ taxonomy/
Google-taxonomy replacement, orders/parcel workspace, category-price-outlier plan, this
Market Rules turnover itself). None of it is committed. Do not `git add -A` and do not
assume a clean tree. If you need to commit vault changes, stage only the files relevant to
what YOU changed, and per this repo's own git-safety rule (see hot.md), commits go through a
branch + PR, never straight to a shared branch - and per AGENTS.md, do not commit or push
anything without the owner (Bogs) explicitly asking first.

Sals3-portal codebase:
E:\sals3-portal, branch `develop`, HEAD `9186730`, clean working tree, in sync with
origin/develop (just fast-forwarded 2 commits before this handoff).

OBSIDIAN VAULT

Vault root: E:\sals3-ecommerce\docs
Canonical notes directory: E:\sals3-ecommerce\docs\Wiki\wiki

Read these first, in this order:
1. hot.md (just updated 2026-08-14 - has the current, corrected Market Rules summary)
2. agent-operating-contract.md
3. nextjs-component-security-code-rules.md
4. project-structure-installation-and-runbook.md
5. ADR-015-commercial-pricing-governance-category-product-and-fx-adjustments.md (READ THE
   2026-08-14 AMENDMENT AT THE END, not just the original approved text - the original
   [!IMPORTANT] callout says "not implemented", which is now stale; the amendment explains why)
6. sals3-portal-seller-market-configuration.md (READ THE 2026-08-14 UPDATE SECTION AT THE
   END - the body above it is accurate only as of 2026-08-12)
7. sals3-session-2026-08-11-part34-category-margin-and-fx-policy.md (ADR-015 Phase 1 build)
8. sals3-session-2026-08-13-part41-market-rules-profile-fix-and-pricing-rework.md (the two
   undocumented follow-on commits, reconstructed today)

Reusable engineering lessons are in sals3-skills.md (69+ entries as of the last count this
prompt's author saw - re-verify the exact count yourself).

CURRENT VERIFIED STATE OF MARKET RULES (verified 2026-08-14 by reading the actual code)

`/market-rules` (src/app/(portal)/market-rules/page.tsx) renders, in order:
- MarketProfileSection - always shown to anyone with `market_rules:read`. Reads a real
  `seller_market_profiles` row for `session.sellerId`. DRAFT -> ACTIVE -> SUSPENDED lifecycle,
  gated by `market_profile:manage` (admin/seller_manager only), compare-and-set on
  (status, version), business-reason-required, audited same-transaction. Real Dialogs
  (MarketProfileTransitionDialog, BeginMarketProfileSetupDialog) - NOT reworked to be
  dialog-free, unlike the pricing section below.
- MarketRolesExplainerPanel - always shown, static.
- CategoryPricingSection + FundingBufferSection - gated independently on `pricing_policy:read`
  (narrower than `market_rules:read`).

Category pricing: 1,345 leaf categories (Sals3 Taxonomy v0) grouped into 226 expandable
L1>L2 groups (CategoryMarginGroupRow/CategoryMarginLeafRow). A leaf commits inline on first
edit; a group-level bulk overwrite needs an inline arm/confirm step (this replaced an older
modal-dialog flow - see part41). "Funding buffer" (renamed from "FX adjustment" in the same
rework) is a single seller-owned buffer, edited inline, with a Deactivate action still behind
a confirm Dialog (destructive actions keep dialogs; edits don't).

Resolver (src/modules/pricing/resolver.ts): category -> product override -> variant override
precedence, most-specific-wins; funding buffer applied on top. IMPORTANT GAP: no real
cross-currency reference-FX provider is approved yet, so the funding-buffer branch only ever
sees an identity (1.0) rate for same-currency pairs - the whole non-identity FX-adjustment
code path is real, tested, but practically dead until a provider is approved.

Capability module (src/modules/market-config/capabilities.ts):
CAPABILITY_VERSION = 'seller-market-capability-v2-au-ph-usd-publishable' - this is v2, up
from the v1 the vault previously documented. `authorizedSellingCurrencyCodes: ['USD']` is
now populated for both AU and PH (used to be empty for both). Nobody has written down WHY or
WHEN v1 became v2 - that's a real gap in the historical record, not something this prompt can
answer for you.

Migrations relevant to Market Rules, in order: 0011_curious_falcon.sql (ADR-015 Phase 1
schema - originally numbered 0010, renumbered), 0012_flashy_penance.sql (seller_market_profiles),
0018_rare_william_stryker.sql (drops now-unused FX columns/enum from
pricing_fx_adjustment_policies - a real DROP COLUMN/DROP TYPE, not purely additive like the
others). The portal's own README (added 2026-08-14) reports production
seller_market_profiles/pricing_category_policies as empty-but-queryable, which implies these
migrations ARE applied to production - but this was NOT independently re-verified against a
live database by the agent who wrote this prompt, only read from the repo's own first-party
doc. Confirm this yourself before relying on it, and ask Bogs how/when production actually got
migrated (no CI-driven migration step exists in this repo per project-structure-installation-
and-runbook.md - it's manual `npm run db:migrate`).

Tests run today (2026-08-14) by the investigating agent: `npm run typecheck` clean;
`npx vitest run` scoped to the market-rules/pricing/market-config test files - 15 files, 190
tests, all passed. The FULL `npm run verify` (lint/format/build/full unit+e2e/audit) was NOT
re-run against the two undocumented commits (6e5b244, 91b5265, 166d0ec) - the last full green
verify on record for this feature area is against 973fa0e specifically (part34's own evidence,
728 passed/4 skipped + 51 e2e passed at that time). Run the full `npm run verify` yourself
before trusting that everything downstream of those two commits is still fully green.

LATEST COMMIT SCOPE (all already merged to `develop`, most recent first)

- 9186730 (merge, tip) / 2e6e131 - unrelated: publish flow, seller retail prices + pilot
  destination fallback.
- 166d0ec (PR #63) - Market Rules pricing rework: dialog-free inline category editing,
  "FX adjustment" -> "Funding buffer" rename (UI + resolver's typed reasons), migration
  0018 dropping now-unused FX columns/enum, PolicyHistoryButton audit popover.
- 91b5265 - fix(orders): /orders reads the real market profile table instead of the
  illustrative fixture (same fix class as market-rules got on 2026-08-12).
- 6e5b244 (PR #39) - real per-seller market profile: seller_market_profiles table,
  src/modules/market-config/, DRAFT/ACTIVE/SUSPENDED lifecycle, capability boundary.
- 973fa0e (PR #37) - ADR-015 Phase 1: pricing-policy schema, money-math, resolver, Sals3
  Taxonomy v0 seed data, server actions, original (dialog-based) pricing UI.

NON-NEGOTIABLE SALS3 ARCHITECTURE RELEVANT TO THIS AREA
- ADR-015's platform/merchant authority split is the load-bearing rule for anything you touch
  here: Admin Portal owns only platform reference-FX config, Sals3 fees/costs, enabled
  capabilities, and safety/legal guardrails. Seller Portal owns merchant margins, category
  PIC assignments, and the merchant's own funding buffer. Never let a change blur that line.
- Read agent-operating-contract.md and nextjs-component-security-code-rules.md before any
  code/config/test edit in this repo - non-negotiable per AGENTS.md at repo root.
- Run `npm run verify` before reporting any code work in this area complete.
- Do not deploy, publish, push, or commit (either repo) unless Bogs explicitly asks.

CORE DATA-INTEGRITY RULES
- Tenant identity always comes from `session.sellerId` - no server action in this module
  accepts a caller-supplied seller id, so there is nothing to forge.
- Every policy table (pricing_category_policies, pricing_product_overrides,
  pricing_variant_overrides, pricing_fx_adjustment_policies) is append-only-by-edit: an edit
  inserts a new row and marks the old one SUPERSEDED in the same transaction; a partial unique
  index (WHERE status = 'ACTIVE') is what actually enforces "one current policy," not app code.
- seller_market_profiles transitions use compare-and-set on the exact (status, version) the
  page rendered from - a stale/replayed submit is rejected, not silently overwritten.
- Every write appends an audit_events row in the SAME transaction as the state change.

CURRENT OPERATIONAL FLOW

A. Market profile setup: seller hits /market-rules with no profile -> "successful-empty"
   state (may offer setup if authorized) -> beginMarketProfileSetupAction creates a DRAFT
   (only for a destination that's both in the capability allowlist AND globally permitted by
   the buyer-destination policy) -> activateMarketProfileAction / suspendMarketProfileAction
   move it through ACTIVE/SUSPENDED, each a compare-and-set + audited transaction.
B. Category/product pricing resolution: resolver.ts resolves category policy, then checks for
   a more specific product override, then a variant override (most specific wins), then
   applies the seller's funding buffer on top of a reference rate that is currently always
   1.0 (identity) because no real FX provider is approved. Returns a typed unavailable-reason
   when anything required is missing - never a fabricated number.

WHAT IS NOT IMPLEMENTED YET / KNOWN GAPS (do not claim otherwise)
- No seller has actually used Market Rules pricing in production yet (per the portal's own
  README, both key tables are empty there as of 2026-08-14).
- pricing_category_policies/pricing_product_overrides/pricing_variant_overrides are still
  keyed to supplier_candidates.id, even though a REAL canonical Product/Variant/Offer model
  now exists in this codebase (added by an unrelated migration, 0013_cold_timeslip.sql).
  Nobody has revisited this keying decision - it is the single most concrete "next step" this
  investigation surfaced, if the goal is to make pricing usable against real products.
- The funding-buffer/FX-adjustment code path is real and tested but practically unreachable
  (see "reference-FX provider" gap above).
- Payments, logistics/freight quoting, tax treatment, and payouts remain explicitly
  out-of-scope/unbuilt for both AU and PH per the capability module's own
  `pendingCapabilities` list - "offerable" in the capability sense never means "launched."
- sals3-manual-testing-checklist.md currently has ZERO items tracking Market Rules/pricing -
  if you ship anything here, add real checklist items, don't assume someone else will.
- No note anywhere records why/when the capability module moved from v1 to v2.

MANUAL ACCEPTANCE STILL OPEN
None tracked in sals3-manual-testing-checklist.md today - this is itself a gap, not a sign
everything's covered. Add real items if you ship changes here.

KNOWN REAL FACTS (verified 2026-08-14, not carried forward from stale notes)
- Capability version: seller-market-capability-v2-au-ph-usd-publishable.
- authorizedSellingCurrencyCodes: ['USD'] for both AU and PH.
- 1,345 leaf categories / 226 L1>L2 groups in the category pricing UI.
- 15 test files / 190 tests scoped to this feature area, all passing as of 2026-08-14
  (typecheck + scoped vitest only - full verify not re-run today).
- Portal README (2026-08-14) claims production seller_market_profiles and
  pricing_category_policies are both empty-but-queryable - not independently re-verified
  against a live database by this prompt's author.

IMPORTANT USER DECISIONS AND PARKED IDEAS
Approved: ADR-015's decision framework (platform/merchant authority split, layered pricing
precedence, customizable merchant FX/funding buffer, category-PIC model) - approved by Bogs
2026-08-10, but the ADR's own text explicitly did NOT authorize building either pricing
surface. Phase 1 was built anyway (973fa0e) on the strength of the approved design; that
tension is recorded in the ADR's own 2026-08-14 amendment, not resolved by it - it's an owner
judgment call whether that was the right call, not a fact to assert either way.
Not confirmed as a separate explicit owner decision (as far as this investigation found): the
FX-adjustment -> funding-buffer rename and the dialog-free UI rework (166d0ec) - it shipped
via a reviewed, green-CI PR, but no session note or vault record shows Bogs specifically
approving that rename/rework ahead of time. Worth asking Bogs directly if that matters to you.
Parked: see parked-ideas-backlog.md - do not silently unpark anything from there.

DESIGN AND UX RULES
- Category/funding-buffer EDITS are inline, no modal (as of the 166d0ec rework).
- DESTRUCTIVE actions (Deactivate category policy, Deactivate funding buffer, Market Profile
  transitions) still use confirm Dialogs - don't remove those while making other things
  dialog-free; the commit history draws that line deliberately.
- PolicyHistoryButton is a shared, lazy-fetch audit-history popover - reuse it rather than
  building a second history UI if you add more policy types.

GIT AND FILE-SAFETY RULES
- Vault (E:\sals3-ecommerce): has a large uncommitted, multi-topic working tree right now
  (see "Vault working-tree state" above). Do not blanket-stage it. Commits go through a
  branch + PR per the vault's own git-safety rule; never commit straight to a shared branch.
- Sals3-portal codebase: never auto-commit. Every commit needs Bogs's explicit go-ahead,
  every single time - no standing blanket approval carries forward from a prior session.
- Neither repo gets pushed without Bogs explicitly asking, per AGENTS.md.
- Ask whether to write/update a turnover prompt right after any commit to either repo's git.

OBSIDIAN MAINTENANCE RULE
After any material Market Rules decision or implementation:
1. Update hot.md with verified current state only (find the two Market Rules bullets under
   "Verified implementation state" and "Active risks and blockers" - both were just
   corrected 2026-08-14, keep them current going forward).
2. Update ADR-015's `implementation_status` frontmatter if the build state changes again.
3. Add a session note for anything you ship - do NOT let another 2-3 commits go undocumented
   the way 6e5b244/91b5265/166d0ec did before this prompt's investigation caught it.
4. Add real sals3-manual-testing-checklist.md items for anything a human should click-test.
5. Do not rewrite historical session notes to pretend old facts were never true - add a
   clearly dated follow-up instead (this is exactly what today's ADR-015 amendment and
   seller-market-configuration.md update did - follow that pattern).

LATEST VAULT PUBLICATION NOTE
This prompt itself, plus the same-day updates it's built from: hot.md, the ADR-015
amendment, the sals3-portal-seller-market-configuration.md update, and the new
sals3-session-2026-08-13-part41-market-rules-profile-fix-and-pricing-rework.md session note.
None of these are committed yet - see GIT AND FILE-SAFETY RULES.

IMMEDIATE TAKEOVER CHECKLIST
Before changing anything:
1. Read hot.md and the canonical notes listed above, in order.
2. In E:\sals3-ecommerce, run: git branch --show-current / git status --short / git log -1
   --oneline --decorate. Confirm it matches what this prompt says (dirty tree, branch
   docs/admin-portal-part39).
3. In E:\sals3-portal, run the same three commands. Confirm branch develop, HEAD 9186730,
   clean tree, in sync with origin/develop. If it's moved on, that's expected - just verify
   for real before trusting anything else in this prompt.
4. Run `npm run typecheck` and the scoped vitest command
   (`npx vitest run src/app/(portal)/market-rules src/modules/pricing src/modules/market-config
   src/components/seller-center/market-rules`) to confirm you're starting from the same green
   baseline this prompt describes, before assuming it still holds.
5. If you find a conflict between code/vault content and this prompt, investigate and correct
   the current canonical note with verified evidence - don't guess, and don't silently trust
   this prompt over what you actually observe.

HANDOFF SUMMARY
Market Rules has grown past what the vault had documented: a real per-seller market profile
lifecycle, a 1,345-category pricing UI reworked once already into a dialog-free inline flow,
and a renamed "funding buffer" concept - all merged to `develop`, all typecheck/scoped-test
green as of today, but none of it launched to a real seller yet and two of its four
commits had no session note until today's reconstruction. The most concrete piece of
unfinished work this investigation surfaced is that pricing overrides still point at
`supplier_candidates` instead of the real Product/Variant model that now exists elsewhere in
the codebase - that's a natural next task if the goal is making this pricing model usable
against real products, but it's your and Bogs's call what "work in Market Rules" means next,
not something this prompt should decide for you.
````
